services:
  highlight2api:
    build: .
    container_name: highlight2api
    ports:
      - "9999:8080"
    volumes:
      - ./config:/app/config
    environment:
      - DEBUG=false
    restart: unless-stopped
